#include "VoxelRoute.h"

// Determines the height of a given voxel at the provided position.
float GetHeightForVoxel(MapInfo* voxelMap, const vmath::vec3i& voxelId, const vmath::vec3& position)
{
    // TODO
    return 0.0f;
}

// Given a voxel, finds all valid voxel neighbors for travel. Returns true if neighbors were found, false otherwise.
static void FindVoxelNeighbors(MapInfo* voxelMap, const vmath::vec3i& voxelId, std::vector<vmath::vec3i>& neighbors)
{
    // TODO
}
